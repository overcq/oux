name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: packages install
      run: sudo apt-get install libxcb-xkb1 libxkbcommon-x11-0
    - name: test
      run: pkg-config --list-all && false
    - name: make
      run: make all
