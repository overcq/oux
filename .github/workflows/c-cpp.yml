name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: packages install
      run: sudo apt-get install libtool libxcb-shape0 libxcb-xkb1 libxkbcommon-x11-0
    - name: make
      run: make all
